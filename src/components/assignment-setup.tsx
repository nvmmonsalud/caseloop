"use client";
import { useState } from "react"; import { caseStudy } from "@/lib/data"; import { Check } from "lucide-react";
const fields=[{label:"Case name",value:caseStudy.title},{label:"Learning objectives",value:caseStudy.objectives.join(" · ")},{label:"Case material",value:"5 grounded case sources attached"},{label:"Decision options",value:"Acquisition · Joint venture · Organic entry"},{label:"Diagnostic questions",value:"Recommendation · Evidence · Biggest uncertainty"},{label:"Faculty rubric",value:"Evidence use · Assumption quality · Trade-off reasoning"}];
export function AssignmentSetup(){const [values,setValues]=useState(fields.map(x=>x.value));const [saved,setSaved]=useState(false);return <details className="mt-12 border-y rule py-6"><summary className="cursor-pointer font-bold">Lightweight assignment setup</summary><div className="mt-6 grid gap-5 sm:grid-cols-2">{fields.map((field,i)=><label key={field.label} className="text-sm font-bold">{field.label}<input value={values[i]} onChange={e=>setValues(current=>current.map((v,j)=>j===i?e.target.value:v))} className="mt-2 w-full rounded-lg border rule bg-white p-3 font-normal"/></label>)}</div><button onClick={()=>{localStorage.setItem("caseflow-assignment",JSON.stringify(values));setSaved(true);setTimeout(()=>setSaved(false),2500)}} className="btn-primary mt-5">{saved&&<Check size={15}/>} {saved?"Assignment saved":"Save assignment"}</button></details>}
