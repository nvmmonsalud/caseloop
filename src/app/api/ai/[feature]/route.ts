import { NextRequest,NextResponse } from "next/server"; import { z } from "zod"; import { runAI,AIServiceError } from "@/lib/ai/service"; import { schemas,type AIFeature } from "@/lib/ai/schemas";
const requestSchema=z.object({input:z.unknown().refine(v=>v!==null&&v!=="",{message:"A response is required."})});
export async function POST(request:NextRequest,{params}:{params:Promise<{feature:string}>}){try{const {feature}=await params;if(!(feature in schemas))return NextResponse.json({error:"Unsupported AI feature."},{status:404});const body=requestSchema.parse(await request.json());const result=await runAI(feature as AIFeature,body.input);return NextResponse.json(result)}catch(error){if(error instanceof z.ZodError)return NextResponse.json({error:error.issues[0]?.message||"Invalid request."},{status:400});if(error instanceof AIServiceError)return NextResponse.json({error:error.message},{status:error.status});return NextResponse.json({error:"Unexpected AI service error."},{status:500})}}
