import { describe,expect,it } from "vitest"; import { coachSchema,briefSchema } from "./schemas"; import { fallbacks,getDemoFallback } from "./fallbacks";
describe("AI structured outputs",()=>{it("validates the coach fallback",()=>expect(coachSchema.safeParse(fallbacks.coach).success).toBe(true));it("validates the preparation brief fallback",()=>expect(briefSchema.safeParse(fallbacks.brief).success).toBe(true));it("uses the student's committed position in demo mode",()=>{const output=getDemoFallback("brief",{student:{position:"Organic entry",rationale:"Learn before scaling",evidence:"Consumer fit [S3]",confidence:62}});expect(briefSchema.parse(output).recommendation).toContain("Organic entry")});it("rejects out-of-range confidence",()=>expect(briefSchema.safeParse({...fallbacks.brief,confidence:120}).success).toBe(false))});
