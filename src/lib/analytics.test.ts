import { describe,expect,it } from "vitest"; import { cohortMetrics } from "./analytics"; import { studentResponses } from "./data";
describe("cohortMetrics",()=>{it("derives every faculty metric from seeded responses",()=>{const m=cohortMetrics();expect(m.total).toBe(12);expect(Object.values(m.positions).reduce((a,b)=>a+b,0)).toBe(12);expect(m.positions).toEqual({Acquisition:3,"Joint venture":6,"Organic entry":3});expect(m.averageConfidence).toBe(74);expect(m.weakHighConfidence).toBe(2)});it("counts evidence citations without fabrication",()=>{const m=cohortMetrics();const citations=studentResponses.flatMap(r=>r.evidence).length;expect(Object.values(m.evidenceCounts).reduce((a,b)=>a+b,0)).toBe(citations);expect(m.evidenceCounts.S5).toBe(6)})});
